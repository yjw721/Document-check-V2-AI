# 增量需求｜文档核验工具能力扩展（主 Prompt 增量模块）

> 本模块为本地离线文档核验工具的功能增量规范，可直接并入主 Prompt。
> 适用对象：Word(.doc/.docx) / Excel(.xls/.xlsx) / PDF(.pdf) 低级错误与表述问题检查工具。
> 强制基调：纯本地、零联网、零数据外发、只读不写、可审计、可离线打包。

---

## 0. 统一数据契约（所有检测项的强制输出结构）

任何检测问题（内置规则 / 自定义规则 / 词库命中）必须统一收敛为以下结构；缺字段视为不合格实现：

```
rule_id    : 规则唯一标识（内置 fmt_*/tn_*；自定义 cust_<group>_<n>；词库 wb_<group>_<n>）
category   : 分类（format_error 格式错误 | expression 表述不当 | custom_rule 自定义规则 | wordbank 词库命中）
scope      : 生效文档范围（all | word | excel | pdf）
location   : 精准位置对象（见 §2 格式约束，必填，不得为空）
snippet    : 原文片段（可空）
suggestion : 整改建议（可空）
severity   : low | medium | high
source     : builtin | custom | wordbank
group      : 规则/词库分组名
tag        : 问题类型标签（custom_rule / wordbank 必填）
state      : normal | ignored | checked
```

前端明细、统计、Word 报告三处必须基于同一 `rule_id + location + category` 渲染，保证一致。

---

## 1. 文档格式支持（全格式）

支持解析：Word(.doc/.docx)、Excel(.xls/.xlsx)、PDF(.pdf)。

强制约束：
- 仅解析**未加密、完整、未损坏**的标准文档。
- 加密文档 / 损坏文档 / 扫描图片型 PDF → 统一判定为**不可解析**，记录进异常文件清单（`status=error`），写明原因；单文件失败不得中断批量任务。
- PDF **仅提取原生可复制文本层**完成检测；**禁止 OCR、禁止识别图片内文字、禁止解析图层内容**。
- 解析依赖本地库（PDF 用 `pypdf` / `pdfplumber` 等），须随项目装进本地 venv / 离线 wheel，**运行时严禁联网下载任何组件**。
- 旧版 OLE(.doc/.xls) 经本地转换或兼容读取；无法读取同样进入异常清单，不报错崩溃。

---

## 2. 全局精准位置定位（强制落地）

所有 `category` 的问题**必须携带 `location`**，前端错误列表与导出 Word 报告均完整展示；缺位置即缺陷。

统一输出标准（展示文案固定，不得自由发挥）：

- **Word(.doc/.docx)**
  `location = { page: int|null, paragraph: str, note?: "页码无法自动识别，请人工核对" }`
  展示文案：`第 N 页 · 第 X 段`；分页无法读取时固定显示：`页码无法自动识别，请人工核对`。
- **Excel(.xls/.xlsx)**
  `location = { sheet: str, row: int, col: int }`
  展示文案（固定格式）：`{sheet}：第 {row} 行，第 {col} 列`。
- **PDF(.pdf)**
  `location = { page: int }`
  展示文案：`第 N 页`。

前端要求：
- 错误列表**常驻展示位置列**，支持按页码 / 行列号快速核对原文。
- 提供“按位置排序 / 按页码筛选”能力。

报告要求：
- Word 报告新增独立【位置信息】列（或独立位置汇总表），统一展示全部问题坐标，与明细一一对应。

---

## 3. 可视化自定义规则管理（离线、本地、可视化）

入口：内置独立前端【规则配置面板】；**禁止用户手改配置文件**，所有操作持久化到本地 `custom_rules.json`（离线、零上传）。

数据模型 `RuleGroup`：
```
RuleGroup = {
  id, name,
  category: format_error | expression,        # 格式校验规则 / 文字表述校验规则
  scope: all | word | excel | pdf,            # 生效文档范围
  enabled: bool,
  rules: [ Rule ]
}
Rule = {
  id, name, enabled: bool,
  match_mode: keyword | regex,               # 关键词精确匹配 / 正则匹配
  pattern: str,                               # keyword=子串；regex=本地 re 模式
  severity: low | medium | high,
  tag: str,                                   # 问题类型标签（必填）
  suggestion: str                             # 整改建议
}
```

操作能力（面板内完成，不外泄）：
- 分组：新建 / 重命名 / 删除 / 整组启用停用。
- 规则：单条新增 / 编辑 / 删除 / 启用停用。
- 匹配：`keyword`=子串精确匹配；`regex`=本地 `re` 匹配（禁止任何网络语义接口）。
- 批量：规则库**导入 / 导出**（JSON），支持跨设备复用。
- 作用域：单条规则可独立设置 `scope`（全部 / 仅 Word / 仅 Excel / 仅 PDF）。

输出闭环：
- 自定义规则命中 → `category=custom_rule, source=custom`，正常进入前端展示、统计汇总、写入 Word 报告，且**完整携带 §2 位置信息**。

---

## 4. 可视化自定义词库管理（独立模块）

配置面板独立拆分【格式正则规则】与【文字词库】两大模块，互不干扰，支持用户自建专属离线词库。

数据模型 `WordBankGroup`：
```
WordBankGroup = {
  id, name,
  module: format_regex | text_word,           # 格式正则规则模块 / 文字词库模块
  scope: all | word | excel | pdf,
  enabled: bool,
  entries: [ Entry ]
}
Entry = {
  id, keyword: str,                          # 匹配关键词 / 短语
  tag: str,                                   # 问题类型标签（必填）
  suggestion: str,                            # 整改建议（必填）
  enabled: bool
}
```

操作能力：
- 多组词库：新建分组、自定义命名、整组**一键启用 / 停用**（不删除数据）。
- 词条结构（强制）：匹配关键词 / 短语 + 问题类型标签(tag) + 整改建议(suggestion)。
- 词条：单条新增 / 编辑 / 删除 / 启用停用。
- 批量：支持 **TXT / CSV 批量导入**（CSV 表头固定：`keyword,tag,suggestion`）。
- 作用域：按组精准限定生效文档类型（全部 / 仅 Word / 仅 PDF / 仅 Excel）。
- 安全：全部本地存储、零网络请求、不外泄。

输出闭环：
- 词库命中 → `category=wordbank, source=wordbank`，**独立分类标识**，同步展示前端、计入统计、写入 Word 报告，并**附带 §2 精准位置信息**。

---

## 5. 合规与验收（逻辑闭环）

- **离线自检**：全仓库 `grep` 无 `urllib / requests / socket / http:// / https:// / cdn / webbrowser` 等外联。
- **持久化**：所有新增读写仅限本地文件（`custom_rules.json`、`wordbanks.json`），无上传、无遥测。
- **位置覆盖**：UI 错误列表与 Word 报告【位置信息】列对全部问题 100% 携带位置。
- **自定义可见性**：自定义规则 / 词库命中在统计、报告、前端均可见且带位置。
- **异常隔离**：加密 / 损坏 / 扫描型 PDF 进入异常清单，批量检测不中断。
- **只读不写**：工具仅检测、仅标记、仅生成报告，绝不自动修改原始文档。
- **可打包**：新增依赖（如 PDF 库）须纳入离线 wheel 与打包说明，保证整体可离线分发。

---

> 并入主 Prompt 后，任何实现产出必须满足 §0 数据契约与 §5 验收项；凡违反“零联网 / 只读不写 / 位置必填 / 异常不中断”任一条，均判为不合格。
